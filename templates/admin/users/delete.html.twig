{% extends 'admin/admin.base.html.twig' %}
{% block content %}
    <div class="card" style="width: 18rem;">

        <div class="card-body">
            <h5 class="card-title">are you sure to delete</h5>
            
           <div class="row">
               <div class="col-3 "> <button type="button" class="btn btn-default" data-dismiss="modal">annuler</button></div>
               <div class="col-3">   {{ form(form) }}</div>
        </div>
    </div>

{% endblock %}

