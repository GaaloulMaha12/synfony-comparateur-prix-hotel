{% extends 'admin/admin.base.html.twig' %}


{% block content %}

    <!-- Breadcrumbs-->
    <ol class="breadcrumb">
        <li class="breadcrumb-item">
            <a href="index.html">accueil</a>
        </li>
        <li class="breadcrumb-item active">Pages</li>
    </ol>
    <div class="row">
        <div class="col-12 col-md-8"><h3>liste des pages</h3></div>
        <div class="col-6 col-md-4">

            <button id="addNewRow" class="btn btn-primary btn-sm" data-toggle="modal" data-target="#myModal">ajouter page</button>
        </div>
    </div>
    <table class="table table-striped table-dark"  >
        <thead>

        <tr>
            <th scope="col" >type page</th>
            <th scope="col" >titre page</th>
            <th scope="col">Action </th>

        </tr>
        </thead>
        <tbody>
        {%for page in pages %}

            <tr>
                <td> {{  page.typepage }}</td>
                <td> {{  page.titrepage }}</td>



                <td><a href="/pages/editpage/{{ page.id }}"> modifier    </a>
                    <a href="/pages/deletepage/{{ page.id }}"> supprimer </a>
                    <a href="/elements/elementList/{{ page.id }}"> Afficher liste élement </a>
                </td>
            </tr>
        {% endfor %}

        </tbody>
    </table>



    {#MODAL ADD#}
    <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <div class="row">
                    <div class="col-sm">ajout  </div>
                </div>
                <div class="modal-body">
                    {{ form(form) }}
                    <div class="row">

                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}